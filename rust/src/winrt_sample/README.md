## Build

```powershell
docker-compose build
COMPOSE_CONVERT_WINDOWS_PATHS=false && docker-compose up -d

# winpty docker-compose exec server powershell
docker-compose exec server powershell

cd .\Users\Administrator\Documents\
```
