/**
  * @brief 文字の特徴量を利用したソートを行います
  * @param [in,out] sort_target ソート対象
  * @param [in] size ソート対象のサイズ
  * @param [in] sort_key ソートするための基準値
  **/
void galm_sort(char** sort_target, size_t size, char* sort_key);
